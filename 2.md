# Mac 端贴边收藏箱 App 方案

## Summary
做一个菜单栏常驻的 macOS 工具：右侧贴边放一个半透明小面板，鼠标移入展开。用户可以把文件/文件夹拖进去按 tab 分类收藏，点击即可用系统默认方式打开。

## Key Changes
- 交互：右侧边缘常驻，展开时向屏幕内滑入；提供全局快捷键作为备用入口。
- 结构：tab 代表用户自定义分类，支持新增、重命名、删除、拖拽排序。
- 数据：用 bookmark 保存文件/文件夹入口，源文件不移动；移除入口只删收藏不删原文件。
- 打开行为：文件按系统默认应用打开，文件夹直接在 Finder 打开。
- 持久化：本地保存所有 tab、入口、排序、窗口位置、上次展开状态。
- 技术：SwiftUI 负责主界面，AppKit 负责菜单栏、悬浮窗、边缘触发、全局热键等系统能力。

## Test Plan
- 验证拖拽导入、点击打开、移除入口三类核心操作。
- 验证重启后能恢复 tab、入口和窗口状态。
- 验证贴边收起/展开、快捷键唤出、跨 Space 行为稳定。
- 验证滚动浏览在 10 个以上入口时仍可用。

## Assumptions
- 先做本地单机版，不做 iCloud 同步。
- 先不做内嵌预览，聚焦“快速收纳 + 快速打开”。
- Tab 创建先用 `+` 按钮和右键菜单。
- 先用 SwiftUI + AppKit 混合方案。
